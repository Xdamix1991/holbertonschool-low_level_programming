/**
*mul - check for upper character
*Description: funcction to do multiplications
*@a: The digit to print
*@b: the digit to print
*Return: Always 0 (Success).
*On error, -1 is returned, and errno is set appropriately.
*/
#include "main.h"
int mul(int a, int b)
{
return (a * b);
}
