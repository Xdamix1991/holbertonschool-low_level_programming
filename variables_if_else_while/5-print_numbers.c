/**
*main - return digit numbers
*Description: 'printf numbers'
*Return: Always 0 (Success)
*/
#include <stdio.h>
int main(void)
{
int n;
n = 0;
while (n < 10)
{ printf("%d", n);
n++;
}
return (0);
}

