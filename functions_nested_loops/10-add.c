/**
*add - print table of nine
*Description: print the add of two numbers
*@a: The number to add
*@b: The number to add
*Return: Always 0 (Success).
*On error, -1 is returned, and errno is set appropriately.
*/
#include "main.h"
int add(int a, int b)
{
return (a + b);
}
